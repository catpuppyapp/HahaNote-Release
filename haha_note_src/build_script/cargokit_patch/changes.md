changes of `options.dart`:
  - change type of `Toolchain` from `enum` to `class` to allow use custom version in the `cargokit.yaml`

changes of `cargokit.yaml`:
  - just put this file into the dir which need specify version of rust toolchain
