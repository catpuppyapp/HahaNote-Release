

---
x fixed maybe) built-in editor bug (code_forge) 20260628:
fixed: I am not sure, I was seeing this bug on my phone and emulators, but today(20260702), I tested on my phone, everything's fine. even the emulators still buggy, but maybe this bug already fixed or just my memeory wrong(maybe it only occurs on emulators?)? idk, but whatever, now should be ok.


built-in editor(code_forge), on android can not use keyboard suggesstion, if use, cursor maybe can not move when text suggessting(text with underscore), and may be cause editor data lose.

how to resolve:
idk how to resolve it, but can set external editor in settings page to avoid use built-in editor as a solution.
