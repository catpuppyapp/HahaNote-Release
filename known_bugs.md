

---
built-in editor bug 20260628:
built-in editor(code_forge), on android can not use keyboard suggesstion, if use, cursor maybe can not move when text suggessting(text with underscore), and may be cause editor data lose.

how to resolve:
idk how to resolve it, but can set external editor in settings page to avoid use built-in editor as a solution.
